env:
  ARCHITECTURE: `echo $VERSION | sed -rn 's/^[^-]*-(.*)$/\1/p'`
  PACKAGE_VERSION: `echo $VERSION | sed -rn 's/^([^-]*)-.*$/\1/p'`

before_install:
  - echo "ARCH-$ARCHITECTURE || PACKAGE_VERSION-$PACKAGE_VERSION"
